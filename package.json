{
  "name": "fluentai-voice-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "./run.sh"
  },
  "dependencies": {
    "msedge-tts": "^2.0.7"
  }
}
